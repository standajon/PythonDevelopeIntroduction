#this will not use the 'sum' variable
def calculate_sum(x, y):
    sum = x + y
    return x + y


#the sum variable is not used either, however it also causes the compilation error
def calculate_minus(x, y):
        sum = x + y
    return x + y

